// Quick test file to clear localStorage - you can delete this after testing
// Open browser console and run: localStorage.clear()
// Then refresh the page to see the rating popup

console.log('To test the rating popup:');
console.log('1. Open browser console (F12)');
console.log('2. Run: localStorage.clear()');
console.log('3. Refresh the page');
console.log('4. Wait 3 seconds and the rating popup will appear!');

export {};
